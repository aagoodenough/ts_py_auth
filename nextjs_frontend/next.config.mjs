/** @type {import('next').NextConfig} */
const nextConfig = {
  // API calls use NEXT_PUBLIC_API_URL from env
  // For local dev, set NEXT_PUBLIC_API_URL=http://localhost:8000
};

export default nextConfig;